set default=0
set timeout=3

menuentry "AetherOS Desktop" {
  linux /boot/vmlinuz root=UUID={{ROOT_UUID}} rw quiet splash
  initrd /boot/initramfs.img
}

menuentry "AetherOS Recovery" {
  linux /boot/vmlinuz root=UUID={{ROOT_UUID}} rw systemd.unit=recovery.target
  initrd /boot/initramfs.img
}
